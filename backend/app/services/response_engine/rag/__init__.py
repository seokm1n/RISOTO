"""대응 원칙 보충용 RAG.

**RAG가 대체하는 것과 대체하지 않는 것**

원칙 블록(principles_data.json의 must/must_not)은 RAG로 바꾸지 않는다. 그건 가드레일이라
항상 같은 것이 들어가야 하고, 검색이 실패했다고 비면 LLM이 기준 없이 쓰게 된다. 유형은
이미 확정된 상태로 들어오므로 유사도로 찾을 대상도 아니다.

RAG가 하는 일은 그 위에 **상황별 보충**을 얹는 것이다. 원칙은 200~400자로 압축돼 있지만
원문에는 훨씬 많은 내용이 있다. "개인정보 유출인데 아직 규모가 확인되지 않은 상황"처럼
구체적인 국면에 맞는 지침을 원문에서 찾아 덧붙인다.

    프롬프트 = 공통 베이스(정적) + 유형 원칙(정적) + 주체 지침(정적) + RAG 보충(선택)
                                    ↑ 없으면 안 되는 것          ↑ 있으면 좋은 것

**검색은 유형으로 먼저 좁힌다.** 전체 코퍼스에서 유사도 top-k를 뽑으면 다른 유형의 지침이
섞여 들어온다. 유형 필터를 건 뒤 그 안에서만 유사도를 본다 - 법령을 결정적 매핑으로
조회하는 것과 같은 이유다.

**각 청크는 원문의 사용 제약(caution)을 달고 다닌다.** 리콜 자료는 미국 CPSC 절차를,
사이버 자료는 NIST 프레임워크 용어를 본문에 담고 있어서, 청크만 떼어 프롬프트에 넣으면
국내 기업 보고서에 미국 절차가 실린다. 제약을 청크에 붙여야 그 사고를 막을 수 있다.
"""
from .store import Chunk, VectorStore, load_store
from .provider import RagPrincipleProvider

__all__ = ["Chunk", "VectorStore", "load_store", "RagPrincipleProvider"]
